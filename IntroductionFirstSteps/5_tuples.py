# tuple is similar to but tuple is immutable
# tuple is used when it never changes or you do not want it to change
# tuple ia defined by a () parenthesis

coordinates = (4, 5)

print(coordinates[0])
print(coordinates[1])

# coordinates[0] = 2 # this gives error because we cannot edit the values of a tuple

coordinates = (4, 'adsads', 'asda')

print(coordinates[0])
print(coordinates[1])
print(coordinates[2])
